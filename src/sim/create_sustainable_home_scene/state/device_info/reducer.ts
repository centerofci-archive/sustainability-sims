import { AnyAction } from "redux"
import { update_substate } from "../../../../utils/update_state"

import { SustainableHomeRootState } from "../state"



export const device_info_reducer = (state: SustainableHomeRootState, action: AnyAction): SustainableHomeRootState =>
{

    return state
}
