import { AnyAction } from "redux"
import { update_substate } from "../../../../utils/update_state"

import { RootState } from "../state"



export const device_info_reducer = (state: RootState, action: AnyAction): RootState =>
{

    return state
}
